% Generated by: python -m abnuts.analysis.report
% Source result root: results/raw
% Smoke-only evidence: existing rows are too small for paper-scale claims (seeds=1, max steps=2, max chains=8).
\paragraph{Sensitivity and worst-group evidence.}
Smoke-only evidence: existing rows are too small for paper-scale claims (seeds=1, max steps=2, max chains=8).

The lowest observed worst-group mean speedup is 0.777 for predictor mode = unsorted across 1 rows. The worst single-row speedup in that group is 0.777.

Quality guardrails over included speedup rows report 0 total divergences and maximum realized tree depth 2.

Posterior-diagnostic coverage: 16 rows from 2 diagnostics files; max $\widehat{R}$ = 3.200, min bulk ESS = 12.9, min tail ESS = 15.9.

\input{tables/table_sensitivity_worst_groups}
\input{tables/table_sensitivity_coverage}
