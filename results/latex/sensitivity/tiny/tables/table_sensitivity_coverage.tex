% Generated by: python -m abnuts.analysis.report
% Source result root: results/raw
% Smoke-only evidence: existing rows are too small for paper-scale claims (seeds=1, max steps=2, max chains=8).
\begin{table}[t]
\centering
\caption{Sensitivity coverage notes. Missing strata are reported here rather than silently dropped.}
\label{tab:sensitivity-coverage}
\begin{tabular}{lll}
\hline
Requested stratum & Status & Note \\
\hline
depth quantile & collapsed & Depth quantiles are collapsed because all included rows have the same maximum realized tree depth. \\
divergence status & missing divergent group & No included tiny summary row reported divergences. \\
model family metadata & partial & 35 rows used the model name as the model-family fallback. \\
posterior diagnostics & partial & Loaded 16 diagnostic rows from 2 diagnostics.csv files; speedup summaries still lack ESS/s fields. \\
\hline
\end{tabular}
\end{table}
